# File-renamer
This Python script provides a simple utility to rename a file using the os module. It takes the original file path and the new file name as inputs, checking if the files exist and handling potential errors during the renaming process.
